# Per-keyboard Kconfig applied to all Totem shield targets.
# Keep USB logging off by default; flip to y temporarily for debugging.
CONFIG_ZMK_USB_LOGGING=n

# Required for the MOUSE layer (&mmv, &msc, &mkp behaviors).
CONFIG_ZMK_POINTING=y
